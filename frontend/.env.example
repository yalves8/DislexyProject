# In development, leave VITE_API_URL unset — requests go through the Vite proxy.
# In production, set this to your deployed backend URL (no /api suffix).
# VITE_API_URL=https://your-backend-domain.com
